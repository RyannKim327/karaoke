{
  "name": "karakokey",
  "version": "1.2.0-test",
  "author": "RyannKim327",
  "scripts": {
    "backend": "npx gh-pages -d ./backend -b backend",
    "predeploy": "cd frontend && npm run build",
    "deploy": "gh-pages -d frontend/dist -b frontend && cd .."
  },
  "dependencies": {
    "gh-pages": "^6.3.0"
  }
}
